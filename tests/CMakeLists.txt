# Unit and integration tests (Qt Test). Run with: ctest --test-dir build --output-on-failure
function(classboard_test name)
    add_executable(${name} ${name}.cpp)
    target_link_libraries(${name} PRIVATE classboard_lib Qt5::Test)
    add_test(NAME ${name} COMMAND ${name})
    # Widgets tests run without a display. On Windows the offscreen platform uses a FreeType
    # font database that only looks in <Qt>/lib/fonts (absent in normal Qt installs), so point
    # it at the system fonts; otherwise text renders no glyphs.
    set(env "QT_QPA_PLATFORM=offscreen")
    if(WIN32 AND DEFINED ENV{WINDIR})
        file(TO_CMAKE_PATH "$ENV{WINDIR}/Fonts" fontdir)
        list(APPEND env "QT_QPA_FONTDIR=${fontdir}")
    endif()
    set_tests_properties(${name} PROPERTIES ENVIRONMENT "${env}")
endfunction()

classboard_test(tst_geometry)
classboard_test(tst_expression)
classboard_test(tst_document)
classboard_test(tst_serialization)
classboard_test(tst_objects)
classboard_test(tst_mathlayout)
classboard_test(tst_export)
classboard_test(tst_input)
classboard_test(tst_ui)
classboard_test(tst_tools_ui)
classboard_test(tst_pages)
classboard_test(tst_precision)
classboard_test(tst_magic)
classboard_test(tst_board_ui)
